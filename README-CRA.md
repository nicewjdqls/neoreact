# Neo React (CRA variant)
- This variant uses `react-scripts` instead of Vite.
- Entry: `src/index.jsx`
- HTML shell: `public/index.html` (no module script tag)

## Run
npm i
npm start

## Build
npm run build
